
## Shared project counters

Run `supabase-schema.sql` in the Supabase SQL Editor before deploying. The site uses the Supabase project URL and publishable key in `script.js` to read shared project totals and record one view or like per browser/device.
